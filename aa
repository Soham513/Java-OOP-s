# Install OpenGL libraries for libGL.so.1 and other system dependencies
RUN yum -y update && yum install -y \
    mesa-libGL \
    mesa-libGLU \
    glib2 \
    libSM \
    libXext \
    libXrender \
    && yum clean all
